{
  "description": "Fail-closed write-boundary hook: matched file tools may write drafts, with a guarded published-report path.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/publish-gate.mjs"
          }
        ]
      }
    ]
  }
}
