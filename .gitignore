node_modules/
*.log
.DS_Store
Thumbs.db

# Generated output. Anchored with a leading slash on purpose: an unanchored
# `ledger/` also matches src/ledger/, which silently kept the entire receipt
# implementation out of the repository. A clean clone failed six of twelve
# verification steps before that was caught.
/ledger/
/reports/drafts/
/reports/published/
/reports/audit/

# RepoWise generated index state
.repowise/
.claude/CLAUDE.md

# Agent working plans -- docs/ tracks product documentation only
docs/superpowers/

# Editor config: share the extension recommendation, keep machine-specific MCP paths out
.vscode/*
!.vscode/extensions.json

# Build notes that name the originating pursuit; they live outside the repo.
SPEC.md
.briefs/
.agentlogs/
